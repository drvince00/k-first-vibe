name = "k-first-vibe"
compatibility_date = "2026-02-08"
pages_build_output_dir = "dist"

# Run Functions near OpenAI servers (US) instead of nearest edge
# Prevents 403 "unsupported_country_region_territory" errors
[placement]
mode = "smart"

# Secrets are in .dev.vars (local) and Cloudflare Dashboard (production)
# OPENAI_API_KEY, POLAR_ACCESS_TOKEN
